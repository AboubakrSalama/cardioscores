{
  "name": "cardioscores",
  "version": "1.0.0",
  "description": "Cardiology risk score calculators — web + Capacitor (Android/iOS)",
  "private": true,
  "scripts": {
    "serve": "npx --yes serve www",
    "test": "node test/smoke.js",
    "cap:sync": "npx cap sync",
    "android": "npx cap open android",
    "ios": "npx cap open ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/browser": "^7.0.5",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  }
}
